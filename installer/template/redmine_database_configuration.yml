production:
  adapter: postgresql
  database: ${postgresql_database}
  host: ${postgresql_host}
  port: ${postgresql_port}
  username: ${postgresql_user}
  password: ${postgresql_password}
  encoding: utf8
  pool: 50
  reconnect: true

development:
  adapter: postgresql
  database: ${postgresql_database}
  host: ${postgresql_host}
  port: ${postgresql_port}
  username: ${postgresql_user}
  password: ${postgresql_password}
  encoding: utf8
  pool: 50
  reconnect: true

test:
  adapter: postgresql
  database: ${postgresql_database_test}
  host: ${postgresql_host}
  port: ${postgresql_port}
  username: ${postgresql_user}
  password: ${postgresql_password}
  encoding: utf8
  pool: 50
  reconnect: true
${REDMINE_DATABASE_CONFIGURATION_EXTRA}
